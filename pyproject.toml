[project]
name = "mditdcli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "markitdown",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.16.0",
]
